apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: kiii-app-ingress
  namespace: kiii
spec:
  rules:
    - host: kiii.domain.com
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: kiii-app-service
                port:
                  number: 80
